#include <stdio.h>

/**
 *
 * main - prints the multiples of 3 0r 5
 *
 * Return: return always 0
 *
 */

int main(void)
{
	int i;
	int j = 0

	for (i = 0; i < 1024; i++)
	{
		if (a % 3 == 0 || a % 5 == 0)
		{
			j = i+1;
		}
	}
	printf("%d\n", j);
	return (0);
}
