#include <stdio.h>

/**
 * add - sum of two int
 * @a: first num
 * @b: second num
 * Return - on sucess return a + b
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - subtraction of two nums
 * @a: first num
 * @b: second num
 * Return - on sucess return a - b
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - multiplication of a and b
 * @a: first num
 * @b: second num
 * Return - return multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * div - division of a /b
 * @a: first num
 * @b: second num
 * Return - return a / b
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - mod of a and b
 * @a: first num
 * @b: second num
 * Return - return mod of a % b
 */
int mod(int a,  int b)
{
	return (a % b);
}
int main(void)
{
	return (0);
}
