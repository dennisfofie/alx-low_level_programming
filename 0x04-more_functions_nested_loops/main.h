#ifndef _MAIN_H_
#define _MAIN_H_
/**
 * main header file creating
 *
 * Return : sucess or error
 *
 */
int _putchar(char c);
/**
 * print the character c to the stdout
 */
int _isupper(int c);
/**
 * check for uppercase characters and print it to stdout
 *
 * Return: 1 if success
 * Error: 0 if error
 */
int _isdigit(int c);
/**
 * print the digit to the stdout
 *
 * Return: 1 if success
 * Error: 0 if error
 */
int mul(int a, int b);
/**
 * prints the product of two integers
 */
void print_numbers(void);
/**
 * prints an integer from 0 to 1
 */
void print_most_numbers(void);
/**
 * print an integer to the stdout witheout 2 and 4
 */
void more_numbers(void);
/**
 * prints the 10x an integer to the stdout
 */
void print_line(int n);
/**
 * prints a character four times to stdout
 */
void print_diagonal(int n);
/**
 * prints a character to the stdout to diagonal
 */
void print_square(int size);
/**
 * print a size of square
 */
void print_triangle(int size);
/**
 * prints a triangle with a specified size
 */
void print_number(int n);
/**
 * prints an integer to the stdout
 */
#endif

