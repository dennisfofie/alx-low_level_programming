#include "main.h"

/**
 * is_prime_number - print a prinme number
 *
 * @n: the number to checked
 *
 * Return: prime number
 */
int is _prime_number(int n)
{
	if (n / 1 == n && n / n == 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
