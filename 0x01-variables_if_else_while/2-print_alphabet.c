#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/* adding more headers */

/* defining a function main */
/* entry point = main */
	int main(void)
{
/* initializing a variable a */
		char a;
	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar('\n');
/* return : values 0 */
		return (0);

}
